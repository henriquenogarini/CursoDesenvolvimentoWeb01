Neste projeto aprendemos a utilizar os for loops em JavaScript
Utilizando for(i = 0, i < n, i++)
Utilizamos o for neste exercício para imprimir uma lista de cores.
Para enviar uma mensagem que foi digitada, para dentro de uma array, utilizamos colorList.push(userColor).
Tammbém utilizamos novamente o getElementById com InnerHTML para enviar a lista para o html e reproduzir na interface do website.
Há algumas alterações no css.